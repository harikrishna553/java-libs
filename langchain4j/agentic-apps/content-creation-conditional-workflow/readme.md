java -jar ./target/parallel-code-reviewer-1.0.0.jarsequential-handoff-pattern-typed-agents-1.0.0.jar## Setup and Run

Build the application using Maven:

```bash
mvn clean package
```
Run the generated executable JAR:

```java
java -jar ./target/content-creation-conditional-workflow-1.0.0.jar
```

Enter a topic when prompted. 
For example: Please draft an email to all engineers asking them to reset their passwords immediately as a precautionary measure following a potential password leak.