Build the application using Maven:

```bash
mvn clean package
```
Run the generated executable JAR:

```java
java -jar ./target/langchain4j-declarative-output-demo-1.0.0.jar
```

Enter a story idea when prompted. 
For example: Write a short story about a robot learning to play cricket