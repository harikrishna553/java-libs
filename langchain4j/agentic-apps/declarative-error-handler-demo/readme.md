Build the application using Maven:

```bash
mvn clean package
```
Run the generated executable JAR:

```java
java -jar ./target/declarative-error-handler-demo-1.0.0.jar
```

Enter a story idea when prompted. 
For example: A lost bird find its family.