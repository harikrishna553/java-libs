Build the application using Maven:

```bash
mvn clean package
```
Run the generated executable JAR:

```java
java -jar ./target/declarative-activation-condition-workflow-1.0.0.jar 
```

Enter a topic when prompted. 
For example: Create a LinkedIn post about the benefits of AI agents.